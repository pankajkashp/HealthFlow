[build-system]
requires = ["setuptools>=70.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "healthflow-safety"
version = "0.1.0"
description = "HealthFlow safety layer — deterministic input validation, permission checks, and safety gates."
requires-python = ">=3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
