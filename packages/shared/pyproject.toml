[build-system]
requires = ["setuptools>=70.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "healthflow-shared"
version = "0.1.0"
description = "HealthFlow shared foundation — common technical types with no domain semantics."
requires-python = ">=3.13"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
